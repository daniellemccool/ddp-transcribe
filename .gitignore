# Node (crawler tool)
node_modules/

# Rust (future pipeline)
target/
**/*.rs.bk
Cargo.lock.bak

# Pipeline runtime data — never commit these
inbox/
transcripts/
state.sqlite
state.sqlite-wal
state.sqlite-shm
ddp-run-export.sqlite
manifest.parquet
*.tmp

# Downloaded whisper.cpp model binaries (large; fetched via scripts/fetch-tiny-model.sh)
models/

# Runtime logs
process.log

# Operator-local notes (each developer keeps their own)
testnotes.txt

# Operator-local archive (personal drafts, grant applications, etc.)
# Tracked exception: followups-resolved.md is the AD0020 append-only
# resolved-FOLLOWUPS history and stays committed.
docs/archive/*
!docs/archive/followups-resolved.md

# Operator-local working area — us-specific material that must NOT hit the
# public repo: SRC runbooks, bake checklists/notes, dev plans, scratch. Kept
# in-tree because this is where we operate; never committed.
docs/local/

# Real-looking test fixtures kept on disk for ad-hoc development but
# never committed. Synthetic-only fixtures are tracked under their own
# subdirectories.
tests/fixtures/ddp/20260416_test/

# Editor / OS
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Claude session state
.claude/projects/

# Other tool session state
.codex


# Added by cargo

/target
